<UserControl x:Class="BBQRMSSolution.Views.Admin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:AdministrationViewModel, IsDesignTimeCreatable=True}">
  <DockPanel>
    <StackPanel>
      <Button Name="changePin" Padding="4" Margin="1,2,1,2" Click="changePin_Click">
        <StackPanel>
          <TextBlock Text="Change PIN" />
        </StackPanel>
      </Button>
      <Button Name="manageEmployees" Padding="4" Margin="1,2,1,2"
      Click="manageEmployees_Click" Visibility="{Binding Path=SecurityContext.CurrentUser.CanManageEmployees, Converter={StaticResource boolToVisibilityConverter}}">
        <StackPanel>
          <Image Source="/Graphics/system-users.png" Width="32" Height="32"/>
          <TextBlock Text="Manage Employees"/>
        </StackPanel>
      </Button>
    </StackPanel>

    <!-- The current application screen appears here -->
    <ContentPresenter Grid.Row="1" Name="mainContent" Content="{Binding Path=Content}"/>

  </DockPanel>
</UserControl>
